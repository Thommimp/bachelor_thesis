Ordinary Least Squares (OLS) is a linear regression method that estimates parameters by minimizing the sum of squared errors between observed values and predictions \cite{forecasting_book}.

In the simplest case with one feature, OLS fits a line of the form:
\[
y = \beta_0 + \beta_1 x
\]
where $\beta_0$ is the intercept and $\beta_1$ is the slope. These are chosen to minimize:
\[
\min_{\beta_0, \beta_1} \sum_{i=1}^{n} (y_i - (\beta_0 + \beta_1 x_i))^2
\]

Unlike ARIMA, which models temporal dependence using past values of the time series, OLS does not assume any time structure. Each observation is treated as independent.

With multiple features, OLS generalizes to:
\[
y = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + \dots + \beta_p x_p
\]

or in matrix form:
\[
\mathbf{y} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\epsilon}
\]

Here, $\mathbf{X}$ is the feature matrix, $\boldsymbol{\beta}$ is the vector of learned coefficients, and $\boldsymbol{\epsilon}$ is the error term. \\

Each feature contributes additively to the prediction through its own weight, along with a single shared intercept. This allows OLS to incorporate engineered features such as lagged values, rolling statistics, calender effects or external variables. 

\subsubsection{Feature selection}
The same engineered feature pool and forward selection procedure used for the SARIMAX model were applied to the OLS model, as shown in Table~\ref{tab:tested_features} and Algorithm~\ref{alg:algo_1}. The evaluation metric and stopping criterion were also kept identical, ensuring a fair comparison between the two approaches. \\

%At each iteration, an OLS model was fitted using the current selected feature set and one additional candidate feature. The feature producing the largest reduction in validation RMSE was retained. The process continued until no further improvement in performance was observed.

\begin{table}[htbp]
	\centering
	\caption{Feature selection results across seasonal, non-seasonal, and all-cell models}
	\label{tab:feature_selection_ols}
	
	\begin{tabular}{lccc}
		\toprule
		\textbf{Model Type} & \textbf{Selected Features} & \textbf{RMSE} & \text{STD} \\
		\midrule
		Seasonal cells & roll\_mean\_7, lag1, roll\_mean\_14, roll\_std\_14, holiday & 1.45 & 1.42 \\	
		Non-seasonal cells & roll\_mean\_14, lag1, roll\_std\_14, is\_weekend & 1.60 & 1.57 \\
		All cells & roll\_mean\_7, lag1, roll\_std\_7, roll\_mean\_14, is\_weekend  & 1.56 & 1.53 \\
		\bottomrule
	\end{tabular}
\end{table}

Compared to the ARIMA feature selection results, OLS selects a 
larger feature set across all configurations, incorporating 
additional rolling statistics and calendar variables such as 
\texttt{holiday} and \texttt{is\_weekend}. Despite treating 
observations as independent, OLS achieves lower RMSE than 
ARIMA/SARIMAX across all groups (1.45 vs 1.51 for seasonal 
cells, 1.60 vs 1.71 for non-seasonal cells), suggesting that 
the additional features compensate for the lack of explicit 
time-series modelling. This indicates that when sufficiently 
rich features are provided, a simple linear model can match 
or outperform more complex temporal models. \\

\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{figures/OLS_prediction.png}
	\caption{OLS forecasting on site R1 cell 2F}
	\label{fig:OLS_prediction}
\end{figure}

To better understand model behaviour, Figure~\ref{fig:OLS_prediction} 
shows the OLS predictions on the cell used in the ARIMA evaluation (R1, 2F).

